rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /projects/{projectId}/cycles/{cycleId}/issues/{issueId}/evidences/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
